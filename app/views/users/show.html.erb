<h2> Welcome to your homepage, <%= current_user.username %>!</h2>
<%= link_to "Signout", logout_path, method: "delete" %>

<div>
  <table>
    <tr>
      Stadiums in Bucketlist
    </tr>
    <tr>
      <% current_user.areanas.each do |f| %>
        <p> <%= f.name %> </p>
      <% end  %>
    </tr>
  </table>
</div>
